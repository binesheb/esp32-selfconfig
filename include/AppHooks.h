#pragma once

// Add application-specific startup and loop code here.
// The SelfConfig framework calls these functions after provisioning/OTA handling.

inline void applicationSetup() {
  // Example: initialize sensors, LEDs, relays, displays, etc.
}

inline void applicationLoop() {
  // Example: run your device application here.
}
